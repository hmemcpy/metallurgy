# Jing OpenAPI structural navigation

Jing 0.0.5 generates a nested structural type directly from the inline OpenAPI document. The fixture uses Scala 3.7.4,
the version that published artifact targets. The generated `paths`, `/pet`, and `Get` members must remain valid through
the full access chain.
